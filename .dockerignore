# =============================================================================
# Docker Ignore File - Therapy Session Management
# =============================================================================
# Excludes files and directories from Docker build context
# =============================================================================

# Git
.git
.gitignore
.github

# Python cache
__pycache__
*.py[cod]
*$py.class
*.so
.Python

# Virtual environments
venv/
env/
.venv/
.env

# IDE and editors
.idea/
.vscode/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/
coverage.xml
*.cover

# Build artifacts
*.egg-info/
dist/
build/
wheels/
*.egg

# Logs
logs/
*.log

# Local instance data (database files)
instance/

# Documentation
docs/
*.md
!README.md

# Docker files (don't need to copy into image)
Dockerfile*
docker-compose*.yml

# Local development files
# Note: .env is loaded via docker-compose env_file, not copied into image
# .env is NOT excluded here because docker-compose needs it in context
.env.local
.env.*.local

# OS files
.DS_Store
Thumbs.db
